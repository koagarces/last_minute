import { Link } from "react-router-dom";
import MatchesList from "../components/matchList";

const Chat = () => {
  return (
    <div>
      Welcome to Chat
      <MatchesList />
    </div>
  );
};

export default Chat;
